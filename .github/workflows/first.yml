name: first-workflow

on: [push]

jobs:
  first-workflow:
   runs-on: ubuntu-latest

   steps:
    - name: first steps
      run: |
       echo "hello first world"
       echo "hello first time again"
  